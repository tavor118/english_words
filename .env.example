# Google OAuth Client ID for Drive sync (optional).
# Leave unset to run the app in local-only mode.
# Get one at https://console.cloud.google.com/ — see README "Google Drive Sync" section.
VITE_GOOGLE_CLIENT_ID=
